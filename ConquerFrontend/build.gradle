plugins {
	 id "java"
}
java {
    modularity.inferModulePath = true
}
repositories {
	mavenCentral()
}
dependencies {
	testImplementation "org.junit.jupiter:junit-jupiter-api:5.3.1"
	testRuntimeOnly "org.junit.jupiter:junit-jupiter-engine:5.3.1"
	testImplementation "org.hamcrest:hamcrest-library:1.3"
}
compileJava {
	options.fork = true
	options.incremental = true
}
tasks.withType(JavaCompile).each {
	it.options.compilerArgs.add("--enable-preview")
	it.options.compilerArgs.add("--target")
	it.options.compilerArgs.add("15")
}
jar {
	jar.baseName = 'Conquer_frontend'
}
dependencies {
    implementation files('../Conquer/build/libs/Conquer.jar')
    implementation files('../ConquerFrontendSPI/build/libs/ConquerFrontendSPI.jar')
}