import org.apache.tools.ant.taskdefs.condition.Os

task buildExecutable(type: Exec) {
	commandLine "make"
}

clean {
	delete "depreview"
	delete "depreview.exe"
	exec {
		commandLine "make", "clean"
	}
}
